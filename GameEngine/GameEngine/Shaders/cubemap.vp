#version 330

layout (std140) uniform cameraUniform
{
	mat4 vProjection;
	mat4 vView;
};

layout (location = 0) in vec3 vPosition;

out vec3 texcoords;

void main () {
  texcoords = vPosition;
  gl_Position = vProjection * vView * vec4(-vPosition, 1.0);
}