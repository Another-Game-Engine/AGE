#version 330

// (x y) are positions (z w) are UVs
layout (location = 0) in vec4 position;

out vec2 interpolated_texCoord;

void main()
{
	interpolated_texCoord = position.zw; 
	gl_Position = vec4(position.xy, 0.f, 1.0f);
}